api:
  listen: ":1984"
  origin: "*"

rtsp:
  listen: ":8554"

webrtc:
  candidates:
    - stun:8555

ffmpeg:
  bin: ffmpeg

streams:
  camera1: "ffmpeg:${CAMERA_1_URL}#video=h264"
  camera2: "ffmpeg:${CAMERA_2_URL}#video=h264"
  camera3: "ffmpeg:${CAMERA_3_URL}#video=h264"
  camera4: ${CAMERA_4_URL}
